import React from 'react';
import Documents from '../employee/Documents.tsx';

// This component simply renders the employee Documents UI for admin documents
const UIDocuments: React.FC = () => {
  // You can add admin-specific logic or props here if needed
  return <Documents />;
};

export default UIDocuments;
